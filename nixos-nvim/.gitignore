tags
test.sh
.luarc.json
nvim

spell/

# The parent .gitignore ignores nvim-pack-lock.json everywhere (bare pattern,
# applies recursively); un-ignore it here so nvim.nix can read it - flakes
# only see git-tracked files. See :help vim.pack-lockfile
!nvim-pack-lock.json

.DS_Store
